const fs = require("fs"); let content = fs.readFileSync("src/pages/Home.tsx", "utf8"); content = content.replace(/onClick=\{\(\) =>\s*navigate\(`\/planner\?destination=\$\{encodeURIComponent\(query\)\}`\)\}/g, `onClick={() => { const currentLoc = window.prompt("Please enter your current location:"); if (currentLoc) { navigate(\`/planner?source=\${encodeURIComponent(currentLoc)}&destination=\${encodeURIComponent(query)}\`); } else { navigate(\`/planner?destination=\${encodeURIComponent(query)}\`); } }}`); fs.writeFileSync("src/pages/Home.tsx", content, "utf8"); console.log("Done");
